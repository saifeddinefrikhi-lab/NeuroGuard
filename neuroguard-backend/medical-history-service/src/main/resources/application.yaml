spring:
  application:
    name: medical-history-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  mail:
    host: smtp.gmail.com          # or your SMTP server
    port: 587
    username: saifeddinefrikhi@gmail.com
    password: ppco getc azdm crac
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/medical_history_db?createDatabaseIfNotExist=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

jwt:
  secret: XDkzF2YNPA/7vXmPYJmaACjY6VBhwHJbr4pzPF5jguE=

azure:
  storage:
    connection-string: ${AZURE_STORAGE_CONNECTION_STRING:}
    container-name: medical-records
    cdn-endpoint-url: ${AZURE_CDN_ENDPOINT_URL:}
    enabled: ${AZURE_STORAGE_ENABLED:false}

user-service:
  url: ${USER_SERVICE_URL:http://localhost:8081}

keycloak:
  issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/neuroguard}

alert-service:
  url: ${ALERT_SERVICE_URL:http://localhost:8084}

logging:
  level:
    com.neuroguard.medicalhistoryservice.security: DEBUG
    org.springframework.security: DEBUG
upload:
  dir: uploads/medical-history/